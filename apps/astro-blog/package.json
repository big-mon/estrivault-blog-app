{
  "name": "astro-blog",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "pnpm run generate:redirects && astro build && node scripts/generate-markdown-artifacts.mjs",
    "generate:redirects": "node scripts/generate-redirects.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "test:source-markdown": "node --test scripts/generate-markdown-artifacts.test.mjs",
    "lint": "eslint .",
    "format": "prettier --write .",
    "setup:e2e": "playwright install --with-deps chromium",
    "test:e2e": "playwright test",
    "test": "pnpm run test:e2e"
  },
  "dependencies": {
    "@estrivault/content-processor": "workspace:^",
    "@estrivault/og-image-generator": "workspace:^"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.62.1",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.4",
    "tailwindcss": "^4.3.3",
    "typescript": "^6.0.3",
    "yaml": "^2.9.0"
  }
}
