import type { DirectoryLoaderResult } from '../types';

// 仮実装: loadDirectory は配列を返す
export function loadDirectory(options: Record<string, unknown>): DirectoryLoaderResult {
  // 仮実装: 空配列を返す
  return [];
}
